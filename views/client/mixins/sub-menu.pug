mixin sub_menu(records, path)
  ul
    each item in records
      li 
        a(href=`${path}/${item.slug}`) #{item.title}
        if item.children && item.children.length > 0
          +sub_menu(item.children, path)